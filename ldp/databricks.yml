# This is a Databricks asset bundle definition for lakeflow_declarative_pipeline.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: lakeflow_declarative_pipeline
  uuid: c58093d2-a435-46ca-bed3-e6c09dd14c36

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3291448109268428.8.azuredatabricks.net
    variables:
      catalog: tvl_bronze
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://adb-3291448109268428.8.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/ricardo.tjikongo@seacom.com to make sure we only have a single copy.
      root_path: /Workspace/Users/ricardo.tjikongo@seacom.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: tvl_bronze
      schema: prod
    permissions:
      - user_name: ricardo.tjikongo@seacom.com
        level: CAN_MANAGE
